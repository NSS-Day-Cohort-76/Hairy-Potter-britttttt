firePottery = (thrownItem, potteryId) => {
    //must accept following values as input - it needs parameters - in the following order
            //**remember you can add new properties to objects in Javascript
        //an object representing a piece of pottery that was made in the wheel of the makePottery function
         //a number specifying the firing temperature of the kiln
    //must add property of fired with the value of true to the object
    //add property of cracked to object
        //return true if  kiln is above temp of 2200
        //return false if kiln is set to temp at or below 2200
    //after new properties of fired and cracked are added, return the augmented object
    thrownItem.fired = true;
    thrownItem.cracked = true;


}